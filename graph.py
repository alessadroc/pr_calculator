import tkinter as tk

class Graph:
    def __init__(self, root, event):
        self.root = root
        self.event = event
        pass

    def window(root):
        graph_window = tk.Tk(root)
        graph_window.title("Graph")